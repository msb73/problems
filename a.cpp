#include "stdio.h" 
int main(){
      struct salary
      {
        char fn[20];
        float sal;

      };
      struct salary s[10];
      int i = 0;
        scanf("%s", s[i].fn);
        printf("%s",s[0].fn);

        return 0;
        
        }
